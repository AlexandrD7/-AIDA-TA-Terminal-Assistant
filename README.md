# -AIDA-TA-Terminal-Assistant 🤖
AIDA TA - это универсальный интерфейс для работы с языковыми моделями Hugging Face, позволяющий через конфигурационный файл адаптировать систему под любую модель, параметры генерации и аппаратные возможности. Проект решает проблему сложности настройки и интеграции AI-моделей для задач программирования.
<!-- Badges -->

<p align="center">
<img alt="Python" src="https://img.shields.io/badge/Python-3.8%2B-blue?style=for-the-badge&logo=python">
<img alt="License" src="https://img.shields.io/badge/License-MIT-green?style=for-the-badge">
<img alt="Status" src="https://img.shields.io/badge/Status-Active-brightgreen?style=for-the-badge">
</p>

## 🎯 Проблема и Решение

![Проблема](https://img.shields.io/badge/Проблема-red) 

Локальное развертывание и управление LLM для генерации кода сопряжено с рядом трудностей:

**Высокие требования к ресурсам**: Многие модели требуют дорогостоящего оборудования.

**Сложность настройки**: Процесс загрузки, квантизации и настройки параметров генерации требует специальных знаний.

**Отсутствие унификации**: Разные модели требуют разного подхода к интеграции.

**Нет удобного интерфейса**: Взаимодействие с моделями часто ограничивается консолью, что неудобно для анализа и итеративной работы.

![Решение](https://img.shields.io/badge/Решение-green) 

AIDA решает эти проблемы, предоставляя:

**Унифицированный интерфейс**: Веб-интерфейс на Streamlit для всех задач: генерации, анализа, исправления и объяснения кода.

**Гибкая конфигурация**: Единый файл config.json позволяет управлять всем: от выбора модели и устройства (CPU/GPU) до параметров генерации (temperature, top_p) и оптимизаций.

**Оптимизация ресурсов**: Встроенные механизмы квантизации (4-bit/8-bit), управления кэшем и памятью позволяют запускать даже крупные модели (например, 6.7B) на потребительском оборудовании.

**Локальность и безопасность**: Весь процесс происходит на вашей машине, что гарантирует полный контроль и конфиденциальность данных.

## ✨ Ключевые функции

| Функция                     | Описание                                                                 |
|-----------------------------|--------------------------------------------------------------------------|
| **Динамическая загрузка моделей** | Поддержка любых моделей из Hugging Face Hub. Возможность использовать локально сохраненные веса или загружать их из репозитория. |
| **Полная конфигурация**     | Управление всеми аспектами через `config.json`: от выбора модели до порогов мониторинга производительности. |
| **Многозадачность**         | Режимы для генерации, анализа, исправления и объяснения кода с поддержкой системных промптов. |
| **Интерактивный UI**        | Пользовательский интерфейс на Streamlit с вкладками, чатом, мониторингом ресурсов в реальном времени и историей сессий. |
| **Оптимизация производительности** | Поддержка `bfloat16`, 4-битного квантования, `flash-attention` и других техник для ускорения инференса. |
| **Расширенное логирование** | Детальные логи сессий для отладки и анализа работы ассистента. |
| **Персонализация**          | Ассистент запоминает имя пользователя и ведет статистику по выполненным задачам. |

## 🔧 Технологический стек

**Python-библиотеки**: torch, transformers, streamlit, psutil, plotly, accelerate, bitsandbytes

**Streamlit-компоненты**: st.tabs, st.chat_input, st.metric, st.plotly_chart, st.file_uploader

**Инфраструктура**: Локальное развертывание (CPU/GPU), опциональная интеграция с Hugging Face Hub.

## 🛠️ Конфигурация (config.json)

Ниже приведено описание каждого параметра.

## Секция `model`

Управляет выбором и загрузкой модели.

| Параметр           | Описание                                                   | Возможные значения                                                   |
|--------------------|------------------------------------------------------------|----------------------------------------------------------------------|
| `name`             | Идентификатор модели на Hugging Face.                     | `deepseek-ai/deepseek-coder-1.3b-instruct`, `codellama/CodeLlama-7b-hf`, и т.д. |
| `description`      | Человекочитаемое описание для UI.                         | Любая строка, например `DeepSeek Coder 1.3B`                         |
| `size`             | Размер модели для отображения в UI.                       | `1.3B`, `7B`, `6.7B`                                                 |
| `context_size`     | Максимальный размер контекста модели.                     | `4096`, `16384`                                                      |
| `use_local`        | Использовать локальные файлы модели?                      | `true`, `false`                                                      |
| `local_path`       | Путь к папке с локально скачанной моделью.                | `./models/my-model/`                                                 |
| `fallback_to_remote` | Если локальная модель не найдена, загружать с Hugging Face? | `true`, `false`                                                  |
| `integrity_check`  | Проверять наличие ключевых файлов в локальной папке?     | `true`, `false`                                                      |

## Секция `settings`

Общие настройки оборудования и производительности.

| Параметр             | Описание                                                                 | Возможные значения                                                                 |
|----------------------|--------------------------------------------------------------------------|------------------------------------------------------------------------------------|
| `device`             | Устройство для вычислений.                                               | `cuda` (для Nvidia GPU), `cpu`, `auto` (автоматический выбор)                     |
| `cache_dir`          | Папка для кэширования моделей, загруженных из хаба.                     | `./model_cache/`                                                                   |
| `dtype`              | Тип данных для весов модели (влияет на память и скорость).              | `bfloat16`, `float16`, `float32`                         |
| `quantization_type`  | Тип квантизации для снижения потребления VRAM.                          | `4bit`, `8bit`, `null` (отключена)                                                |
| `trust_remote_code`  | Доверять удаленному коду при загрузке модели?                           | `true`, `false`                                                                    |
| `low_cpu_mem_usage`  | Использовать меньше RAM при загрузке модели.                            | `true`, `false`                                                                    |
| `torch_compile`      | *(Экспериментально)* Компилировать модель с `torch.compile` для ускорения. | `true`, `false`                                                                 |
## Секция `generation`

Параметры, управляющие процессом генерации текста.

| Параметр              | Описание                                                                                 | Диапазон        | Рекомендации                                  |
|-----------------------|------------------------------------------------------------------------------------------|------------------|------------------------------------------------|
| `max_new_tokens`      | Максимальное количество генерируемых токенов.                                           | 100 - 4096       | 1200–2000 для кода                            |
| `temperature`         | Креативность vs детерминизм. Низкие значения = более предсказуемый текст.               | 0.1 - 2.0        | 0.2 (для кода), 0.7 (для объяснений)          |
| `top_p`               | Ядерная выборка. Ограничивает словарный запас токенами с совокупной вероятностью.       | 0.1 - 1.0        | 0.95                                          |
| `top_k`               | Ограничивает выборку k наиболее вероятными токенами.                                    | 1 - 200          | 40–50                                         |
| `repetition_penalty`  | Штраф за повторение токенов. Помогает избежать зацикливания.                            | 1.0 - 1.3        | 1.1 – 1.15                                    |
| `no_repeat_ngram_size`| Запрещает повторение n-грамм.                                                           | 2, 3, 4          | 3                                             |
| `do_sample`           | Использовать сэмплирование? Если `false`, используется жадный поиск.                    | `true`, `false`  | `true` для большинства задач                  |
| `num_beams`           | Количество "лучей" при поиске. 1 = без beam search.                                     | 1 - 5            | 1 (быстро), 3–4 (качественнее, но медленнее)  |

## Секция `performance`

Параметры для тонкой настройки производительности и управления ресурсами во время инференса.

| Параметр                   | Описание                                                                                   | Возможные значения / Диапазон      | Рекомендации            |
|----------------------------|--------------------------------------------------------------------------------------------|-------------------------------------|-------------------------|
| `max_history`              | Максимальное число предыдущих обменов (сообщений), сохраняемых в истории общения.          | Любое неотрицательное целое число   | `100`                   |
| `context_window`           | Количество последних сообщений, учитываемых при формировании контекста для модели.         | 1 – 20                              | `5`                     |
| `batch_size`               | Размер пакета (batch) для одновременной обработки токенов во время инференса.             | 1 – 8                               | `1`                     |
| `gradient_checkpointing`   | Использовать ли градиентный чекпоинтинг для снижения потребления оперативной памяти.       | `true`, `false`                    | `false` (для инференса) |
| `memory_efficient_attention` | Включить оптимизированную реализацию механизма внимания для экономии VRAM.              | `true`, `false`                    | `true`                  |
| `compile_mode`             | Режим компиляции модели для уменьшения накладных расходов или максимальной скорости.       | `"reduce-overhead"`, `"full-speed"` | `"reduce-overhead"`     |
| `max_response_length`      | Максимальное число токенов в одном ответе модели.                                         | 1 – 100 000                         | `50 000`                |
| `max_context_length`       | Общая длина контекста (вход + история) в токенах.                                         | 1 – 65 536                          | `4 096`                 |

## Секция `monitoring`

Параметры для сбора метрик, отслеживания производительности и оповещений.

| Параметр                             | Описание                                                                                          | Возможные значения / Диапазон       | Рекомендации               |
|--------------------------------------|---------------------------------------------------------------------------------------------------|--------------------------------------|----------------------------|
| `enable_metrics`                     | Включить сбор общих метрик работы модели и системы.                                               | `true`, `false`                     | `true`                     |
| `memory_monitoring`                  | Включить мониторинг использования оперативной и видеопамяти.                                       | `true`, `false`                     | `true`                     |
| `performance_tracking`               | Включить сбор метрик производительности (время отклика, пропускная способность).                   | `true`, `false`                     | `true`                     |
| `error_reporting`                    | Включить автоматическую отправку отчётов об ошибках и исключениях.                                 | `true`, `false`                     | `true`                     |
| `metrics_retention`                  | Максимальное число сохранённых точек метрик перед удалением старых.                                | Любое целое неотрицательное число    | `1000`                     |
| `alert_thresholds.inference_latency_ms` | Порог задержки инференса в миллисекундах, при превышении которого генерируются оповещения.      | Любое положительное целое (мс)       | `5000`                     |
| `alert_thresholds.memory_usage_mb`    | Порог использования памяти в мегабайтах, при превышении которого генерируются оповещения.         | Любое положительное целое (MB)       | `6144`                     |
| `alert_thresholds.min_tokens_per_second` | Минимальное число токенов в секунду, ниже которого генерируются оповещения о деградации скорости. | Любое положительное целое (ток./с)   | `10`                       |

## Секция `optimization`

Параметры для адаптивной оптимизации ресурсов, автоматического распределения и очистки памяти.

| Параметр                       | Описание                                                                      | Возможные значения | Рекомендации  |
|--------------------------------|-------------------------------------------------------------------------------|--------------------|---------------|
| `adaptive_memory`              | Автоматическая подстройка использования памяти в зависимости от нагрузки.       | `true`, `false`    | `true`        |
| `auto_device_map`              | Автоматическое распределение слоёв модели по доступным устройствам.             | `true`, `false`    | `true`        |
| `memory_cleanup`               | Очистка неиспользуемых буферов и кэша для освобождения памяти после инференса. | `true`, `false`    | `true`        |
| `cache_optimization`           | Оптимизация работы кэша моделей для ускорения повторных запросов.              | `true`, `false`    | `true`        |
| `memory_efficient_attention`   | Использовать вариацию механизма внимания с пониженным потреблением памяти.     | `true`, `false`    | `false`       |

## Секция `text_processing`

Параметры для предобработки входного текста и нормализации.

| Параметр                  | Описание                                                                                     | Возможные значения / Диапазон    | Рекомендации      |
|---------------------------|----------------------------------------------------------------------------------------------|-----------------------------------|-------------------|
| `unicode_normalization`   | Метод нормализации Юникода для приведения символов к единому виду.                            | `"NFC"`, `"NFD"`, `"NFKC"`, `"NFKD"` | `"NFC"`           |
| `max_input_length`        | Максимальное число символов или токенов во входном запросе.                                   | Любое положительное целое число    | `5000`            |
| `sanitize_surrogates`     | Очищать невалидные суррогатные пары Юникода перед обработкой.                                 | `true`, `false`                   | `true`            |
| `encoding_fallback`       | Кодировка по умолчанию при ошибке распознавания входной строки.                               | Строка с названием кодировки       | `"utf-8"`         |

# 💻 Системные требования

Требования зависят от выбранной модели и настроек квантизации.

---

## 🧠 Модель: `deepseek-ai/deepseek-coder-1.3b-instruct` (1.3B)

### ✅ GPU (рекомендуется)
- **VRAM**:
  - `bfloat16/float16`: 4–6 GB
  - `4-битная квантизация`: ~2–3 GB
- **Подходящие видеокарты**:
  - NVIDIA GeForce GTX 1660 / RTX 2060 / 3050 и выше

### 🖥️ CPU
- **RAM**: 8–12 GB свободной оперативной памяти
- ⚠️ Производительность будет значительно ниже, чем на GPU

---

## 🧠 Модель: `deepseek-ai/deepseek-coder-6.7b-instruct` (6.7B)

### ✅ GPU (настоятельно рекомендуется)
- **VRAM**:
  - `bfloat16/float16`: 14–16 GB
  - `4-битная квантизация`: ~5–7 GB
- **Рекомендуемые видеокарты**:
  - NVIDIA GeForce RTX 3060 (12 GB), RTX 3080 / 3090 / 4070 / 4080 / 4090

### 🖥️ CPU
- **RAM**: 16–24 GB свободной оперативной памяти
- ⚠️ Запуск возможен, но будет **очень медленным**

---

## 💡 Совет

> Для работы с крупными моделями на ограниченном по мощности оборудовании используйте 4-битную квантизацию:

```json
"quantization_type": "4bit"
